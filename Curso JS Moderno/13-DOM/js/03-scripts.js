//! Definir elementos por id
const formulario = document.getElementById('formulario'); // Selecciona el primero que encuentra (si hay más de un id con el mismo valor)
const noExiste = document.getElementById('no existe'); // Retorna null
