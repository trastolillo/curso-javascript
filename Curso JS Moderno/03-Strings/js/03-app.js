const producto = 'Monitor 20 pulgadas';
const precio = '30€';

//! Concatenación
console.log(producto.concat(precio));
console.log(producto + ' con un precio de ' + precio);
console.log(producto, 'con un precio de ', precio);
// Template literals
console.log(`El producto es: ${producto} tiene un precio de ${precio}`);
